apiVersion: v1
kind: Service

metadata:
  name: netflix-service
  namespace: netflix

spec:
  selector:
    app: netflix

  type: LoadBalancer

  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP